# Railway-compatible naming
DISCORD_TOKEN=your_discord_bot_token

# ESPN source configuration (from Public-ESPN-API docs)
ESPN_TEAM_ID=9720
ESPN_TEAM_NAME=Vancouver Whitecaps

# Optional fallback provider key (used if ESPN is unavailable or rate-limited)
API_FOOTBALL_KEY=you_api_football_key

# Backward-compatible local alias (optional if DISCORD_TOKEN is set)
DISCORD_BOT_TOKEN=

DISCORD_GUILD_ID=
CHANNEL_ID=your_channel_id
# Optional: forum channel where match-day threads are auto-created.
FORUM_CHANNEL_ID=your_forum_channel_id
WHITECAPS_TEAM_ID=1603
POLL_INTERVAL_SECONDS=30
COMMAND_PREFIX=!
