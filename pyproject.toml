[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "whitecaps-bot"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "discord.py>=2.4.0",
    "aiohttp>=3.10.0",
    "python-dotenv>=1.0.1",
]
